// const { Given, When, Then } = require('@cucumber/cucumber');

// let numeros = [];
// let resultado;

// Given("tengo los números {int} y {int}", function (num1, num2) {
//   numeros = [num1, num2];
// });

// When("los sumo", function () {
//   resultado = numeros.reduce((a, b) => a + b, 0);
// });

// Then("obtengo el resultado {int}", function (expectedResult) {
//   if (resultado !== expectedResult) {
//     throw new Error(
//       `El resultado esperado era ${expectedResult}, pero se obtuvo ${resultado}`
//     );
//   }
// });
